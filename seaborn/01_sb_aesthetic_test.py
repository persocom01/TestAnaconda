import numpy as np
from matplotlib import pyplot as plt
import seaborn as sb

# sb.set_palette(palette, n_colors=None, desat=None, color_codes=False)
# Calling the function without an argument sets the color palette to default.
# There are 6 variation to the default palette:
# deep, muted, pastel, bright, dark, and colorblind
# n_colors=int sets the number of colors before it cycles to the start color.
sb.set_palette('colorblind')
